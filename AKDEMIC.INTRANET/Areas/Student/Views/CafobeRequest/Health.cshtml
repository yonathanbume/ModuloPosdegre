@using AKDEMIC.INTRANET.Areas.Student.Models.CafobeRequestViewModels
@model HealthViewModel
@{
    ViewData["Title"] = "Cafobe";
    ViewData["SubTitle"] = "Nueva Solicitud de Salud";
    ViewData["Breadcrumbs"] = new[] {
new BreadcrumbViewData {Name="Cafobe",Action="Index", Controller="CafobeRequest",Area="Student"},
new BreadcrumbViewData { Name = ViewData["SubTitle"].ToString() }

};
    ViewData["BackUrl"] = Url.Action("Index", "Cafobe", new { area = "Student" });
}
<div class="m-content">
    <input id="Id" type="hidden" value="@Model" />
    <div class="m-portlet">
        <div class="m-portlet__head">
            <div class="m-portlet__head-caption">
                <div class="m-portlet__head-title">
                    <h3 class="m-portlet__head-text">
                        @ViewData["SubTitle"]
                    </h3>
                </div>
            </div>
        </div>
        <div class="m-portlet__body">
            <form class="m-form m-form--state" id="create-form" enctype="multipart/form-data" asp-controller="CafobeRequest" asp-action="AddHealth">
                @await Html.PartialAsync("HealthForms/_HealthForm", Model);
            </form>
        </div>
    </div>
</div>

@section Styles{
    <link href="~/css/views/cafobe/add.css" rel="stylesheet" />
}
@section Scripts{
    <script src="~/js/areas/student/cafoberequest/health.js" type="text/javascript"></script>
}